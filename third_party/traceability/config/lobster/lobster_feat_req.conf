ScoreReq.AssumedSystemReqId {
   to_string = "$(item)@$(version?)"
   to_string = "$(item)"
}

ScoreReq.FeatReq {
   description = description
   tags "req" = derived_from
}

ScoreReq.FeatReqId {
   to_string = "$(item)@$(version?)"
   to_string = "$(item)"
}
